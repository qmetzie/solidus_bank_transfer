<% admin_breadcrumb(t('spree.settings')) %>
<% admin_breadcrumb(link_to plural_resource_name(Spree::Bank), spree.admin_banks_path) %>
<% admin_breadcrumb(t('spree.new_zone')) %>

<% content_for :page_actions do %>
<% end %>

<%= render partial: 'spree/shared/error_messages', locals: { target: @bank } %>

<%= form_for [:admin, @bank] do |bank_form| %>
  <%= render partial: 'form', locals: { bank_form: bank_form } %>

  <div class="clear"></div>

  <%= render partial: 'spree/admin/shared/new_resource_links' %>
<% end %>
